# Automatic account fallback plan

1. Extend the multi-account store with account-scoped credential access and compare-and-swap activation.
2. Add strict terminal-quota classification and a provider stream wrapper under failing tests.
3. Integrate request-scoped token resolution, conditional rollback, and session-associated Host audit records.
4. Add the opt-in setting to the existing account surface.
5. Run focused concurrency tests and the complete repository checks.
